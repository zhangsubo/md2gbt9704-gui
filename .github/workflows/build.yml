name: Build and Release

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    name: Build for ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            platform: windows
          - os: macos-latest
            platform: macos

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Get Version
        id: get_version
        shell: bash
        run: |
          VERSION=$(python -c "import re; content = open('main.py').read(); match = re.search(r'setApplicationVersion\(\"([^\"]+)\"\)', content); print(match.group(1) if match else '1.0.0')")
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Build with PyInstaller (Windows)
        if: matrix.platform == 'windows'
        run: |
          pyinstaller md2gbt9704.spec -y
          powershell -Command "Compress-Archive -Path 'dist/md2gbt9704' -DestinationPath 'dist/md2gbt9704_${{ env.VERSION }}_windows.zip'"

      - name: Build with PyInstaller (macOS)
        if: matrix.platform == 'macos'
        run: |
          pyinstaller md2gbt9704.spec -y
          cd dist
          zip -r "md2gbt9704_${{ env.VERSION }}_macos.zip" md2gbt9704.app
          cd ..

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: md2gbt9704_${{ env.VERSION }}_${{ matrix.platform }}
          path: |
            dist/md2gbt9704_${{ env.VERSION }}_windows.zip
            dist/md2gbt9704_${{ env.VERSION }}_macos.zip
          if-no-files-found: error
